# GUIAppToOpenMultipleExeFiles

Select the apps(1 app at a time) which you want to run on scheduler
You can select multiple files
After selection of apps click lon run apps
It has a feature where it saves the file save.txt file to keep recent files path in it
If you dont want it you can delete it
